@model IEnumerable<IoT_Project_Light_System.Models.Broker>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IPAddress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IPAddress)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BrokerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BrokerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BrokerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
